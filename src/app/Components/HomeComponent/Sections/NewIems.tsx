import React from 'react';

const NewIems = () => {
  return (
    <div>
      <button
        type="button"
        disabled
        className="relative text-white bg-black font-medium rounded-md text-sm px-12 py-2.5 text-center inline-flex items-center me-2 mb-2 "
      >
        NEW + RESTORKE
        {/* Outline wrapper */}
        <span className="absolute border-2 border-black rounded-md pointer-events-none -inset-1"></span>
      </button>

{/* New Products Dispaly Section */}
 </div>
  );
};

export default NewIems;
